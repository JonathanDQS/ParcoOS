Una vez haya corrido build.sh copiar kernel.img 8 a la SD y modificar el
archivo config.txt como esta en src ademas borrar la imagen anterior
probablemente llamada kernel7.img

Once build has run copy kernel8.img into the SD and modify config.txt as in src also delete the previous image called kernel7.img

despues llamar a sudo screen ttyUSB0 115200 o el baudrate seteado en el archivo

cambiaste lo de arm_control = 0x200

usamos multi procesadores el primero imprime hello world y el segundo recibe y envia lo que enviamos

cuidado con tener multiple screen up
screen -ls
y luego kill

o sino cuando en la screen ctrl+a y luego :quit
